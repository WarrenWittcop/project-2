<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">

  <link rel="stylesheet" href="/app.css">

  <style>
    
    body {
      font-size: 14px;
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    header {
      background-color: #333;
      color: white;
      padding: 5px;
      box-sizing: border-box;
      width: 100%; 
    }

    .container {
      display: flex;
      flex-direction: column;
      max-width: 80%; 
      max-width: 100%; 
      margin: 0 auto;
    }

    .nav-links {
      text-align: center;
      list-style: none;
      display: flex;
      gap: 10px;
      margin: 0;
      font-size: 16px;
      justify-content: space-between;
    }

    h2 {
      text-align: center;
    }

    h3 {
      text-align: center;
    }

    .nav-link, input[type="submit"] {
      font-weight:bold;
      text-decoration: none;
      color: white;
      background-color: #333;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .nav-link:hover, input[type="submit"]:hover {
     background-color: #555;
    }

    @media screen and (max-width:700px) {
      .collapsible-containter {
        display: inline;
      }
    }

  </style>
</head>
<body>
  <header>
    <h2>Welcome to Might and Magic 7 character creator!</h2>
    <div class="container">
      <h3>Welcome <%= currentUser.username %>!</h3>
        <ul class="nav-links">
         <li><a class="nav-link" href="/MM7">Home</a></li>
           <% if (currentUser) { %>
            <li><a class="nav-link" href="/MM7/new">Create a Character</a></li>
         <li>
            <form action="/sessions?_method=DELETE" method="POST">
              <input type="submit" value="Log Out"/>
            </form>
          </li>
        <% } else { %>
          <li><a class="nav-link" href="/users/new">Sign Up</a></li>
          <li><a class="nav-link" href="/sessions/new">Log In</a></li>
        <% } %>
      </ul>
    </div>
  </header>
  
</body>
</html>
